[project]
name = "backend-architect-agent"
version = "0.1.0"
description = "AI Backend Architect Agent"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]
